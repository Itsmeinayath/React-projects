import React from 'react'

const UserContext = React.createContext()

export default UserContext;

// When you create a context, you get a Provider and a Consumer.

// The Provider is a component that allows consuming components to subscribe to context changes.

